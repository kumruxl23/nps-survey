# Python
__pycache__/
*.pyc
.pytest_cache/
.hypothesis/

# Tooling state
.kiro/

# Brazil build artifacts
/build

# Infra: deployment scripts may contain account IDs, env vars, secrets.
# Keep ignored except for IAM policy JSON which is referenced by ASR
# and is safe to publish (resource ARNs only, no secrets).
infra/
!infra/iam-policies/
!infra/iam-policies/**

# Local-only files (deployment payloads, ad-hoc setup notes)
deploy_payload.txt
SETUP_CHECKLIST.md

# Stakeholder workbooks (PII) � live in S3, not the repo
*.xlsx

# Outlook emails / saved messages (may contain employee names + PII)
*.msg

# Office temp lock files
~$*

# Ad-hoc tool output / scratch (may contain PII; never publish to mirror)
bandit-results.json
bandit-shepherd.csv
bandit-triage.md
final_output*.json
check_feedback_issues.py

# Local throwaway clone used for reviewer self-serve testing
NPSSurveyAutomation/
